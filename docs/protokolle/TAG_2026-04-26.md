# Tagesprotokoll 2026-04-26

## Was passierte

- Demo lief seit ~13:00 in Ubuntu-VM (steemfork) auf Port 8080 aus der Vorgänger-Session (`session_2026-04-26_1240_condenser-demo`)
- Holger hat die Demo per Browser verifiziert: Condenser rendert sauber, Posts laden, Vote-Werte aufgelöst
- Bekannter Bug: einige Bilder laden nicht (Steemit-Images-Proxy-Issue — gleicher Fix wie damals in Welako geplant)
- Entscheidung: Demo wird zum Spielwiese-Projekt, weitere Features in Folge-Sessions
- Auftrag `condenser-plus-setup`: Repo-Setup + Doku-Struktur
- Phase A: Snapshot in `~/condenser-plus-snapshot-20260426-151705`, Demo-Verzeichnis `/home/holger/dev/condenser-demo` als Source bestätigt
- Phase B: privates Repo `greece-lover/condenser-plus` via `gh repo create` angelegt; gh als User-Binary in VM (kein sudo nötig); GITHUB_TOKEN-Auth via PAT mit `repo`-Scope
- Phase C: Steemit-Origin entfernt, neuer Origin `condenser-plus`, branch `master → main`. Erster Push scheiterte wegen shallow clone — Lösung: orphan-Branch für sauberen Initial-Commit (Option ii aus Phase-A-Bericht). Initial-Commit `57e6854` mit allen Demo-Files + LICENSE/NOTICE/CHANGELOG/README/.gitignore. Push erfolgreich.
- Phase D: docs/-Struktur mit ARCHITECTURE.md, DEPLOYMENT.md, ROADMAP.md, DIFF_FROM_UPSTREAM.md, protokolle/TAG_2026-04-26.md

## Tokens / Dauer

(Pflicht-Block am Ende — wird gefüllt bei /usage)
